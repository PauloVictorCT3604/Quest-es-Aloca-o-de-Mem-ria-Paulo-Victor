#include <stdio.h>
#include <stdlib.h>

int main() {
    
    printf("C: %zu byte(s)\n", sizeof(char));
    printf("D: %zu byte(s)\n", sizeof(int));
    printf("F: %zu byte(s)\n", sizeof(float));
    printf("LF: %zu byte(s)\n", sizeof(double));

return 0;

}
